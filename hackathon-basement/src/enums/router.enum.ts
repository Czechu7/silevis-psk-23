export enum RouterEnum {
  home = '',
  login = 'login',
  student = 'student',
  admin = 'admin',
  table = 'table',
  file = 'file',
  docs = 'docs-dashboard',
  files = 'files',
  notFound = '**',
}
